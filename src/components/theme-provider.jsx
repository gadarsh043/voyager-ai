/**
 * Simple theme provider wrapper (no next-themes dependency).
 * Renders children as-is; can be extended for dark/light later.
 */
export function ThemeProvider({ children }) {
  return children
}
